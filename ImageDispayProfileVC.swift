//
//  ImageDispayProfileVC.swift
//  Alamofire
//
//  Created by HIPL-GLOBYLOG on 8/29/19.
//

//class ImageDispayProfileVC: UIViewController {

    import UIKit
    
    class ImageDispayProfileVC: UIViewController {
        /*
        @IBOutlet weak var displayImg: UIImageView!
        var imageReceipt: UIImage?
        var imageUrl: String?
        
        override func viewDidLoad() {
            super.viewDidLoad()
            self.displayImg.contentMode = .scaleAspectFit
            if let imgUrl = imageUrl {
                print("imgUrl ==",imgUrl)
                VCUtil.setImage(url: imgUrl, imageView: displayImg, placeHolder: "hotel_placeholder")
            }
            else if imageReceipt != nil {
                self.displayImg.image = imageReceipt
            }
            else {
                if expenseDataModel.eXPENSE_CATEGORY_STRING == "M" {
                    VCUtil.setImage(url: expenseDataModel.receiptImage, imageView: displayImg, placeHolder: " ")
                }
                else if expenseDataModel.eXPENSE_CATEGORY_IMGPATH != nil && expenseDataModel.eXPENSE_CATEGORY_IMGPATH != "" {
                    VCUtil.setImage(url: expenseDataModel.eXPENSE_CATEGORY_IMGPATH, imageView: displayImg, placeHolder: " ")
                }
                else if expenseDataModel.receiptImage != nil {
                    
                    VCUtil.setImage(url: expenseDataModel.receiptImage, imageView: displayImg, placeHolder: " ")
                }
                else {
                    //self.displayImg.image = image
                }
                
            }
            
        }

        
        @IBAction func onCross(_ sender: Any) {
            self.dismiss(animated: true, completion: nil)
        }
        
        */
        
}
